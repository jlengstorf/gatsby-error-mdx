# This Is a Gatsby MDX Site

This page was rendered with standard MDX using the `pages/` directory.

The error comes from programmatically generating pages from the `content/` directory.

See [this generated post](/test-post) for the error.
